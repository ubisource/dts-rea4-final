export const Featured = (props) => {
    return (
        <>
            <h3 className="featured">{props.title}</h3>
            <div className="featured-content">
                featured content
            </div>
        </>
    );
};